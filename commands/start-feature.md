---
description: "Start the structured feature development workflow (Discovery → Spec → Plan → Implement → Test)"
---

# /start-feature

Fallback phrase: `Start the feature workflow. Load the feature-workflow skill, read ACTIVE_CONTEXT.md, and begin Phase 1: Discovery (or resume from the last passed gate if a workflow is already active).`

Required skill: `superpowers:feature-workflow`

Read `docs/project-memory/ACTIVE_CONTEXT.md` to check for existing workflow state. If a workflow is already active, offer to resume from the last passed gate. If no active workflow, begin Phase 1: Discovery.
